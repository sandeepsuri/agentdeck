# AgentDeck VS Code Helper

This local companion extension lets AgentDeck identify, focus, and send prompts to exact VS Code integrated terminals. It connects only to the loopback AgentDeck server configured by `agentdeck.serverUrl`.
